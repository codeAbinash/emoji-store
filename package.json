{
  "scripts": {
    "build": "tsc -d && uglifyjs build/index.js -o build/index.js -c -m",
    "prettier": "prettier --check .",
    "prettier-write": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.0.3",
    "typescript": "^5.2.2",
    "uglify-js": "^3.17.4"
  },
  "dependencies": {
    "emoji-store": "^1.0.5"
  }
}
